/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      remotePatterns: [
        {
          hostname: "giddy-pheasant-952.convex.cloud",
        },
      ],
    },
  };

export default nextConfig;
